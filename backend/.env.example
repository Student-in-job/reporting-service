# === App ===
APP_HOST=0.0.0.0
APP_PORT=8000

# === Own DB (config storage) ===
DATABASE_URL=postgresql+asyncpg://report:report@localhost:5432/report_service

# === JWT ===
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# === Seed admin ===
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
